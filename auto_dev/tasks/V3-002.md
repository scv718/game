# V3-002 Current Runtime Asset Bootstrap

Purpose: make the ignored/external Tiny Swords and Quaternius runtime asset closure reproducible from a fresh checkout.

Reuse: existing `tools/download_quaternius_packs.ps1`, `tools/download_tiny_swords.ps1`, generation tools, license/readme policy.

Constraints: do not commit external source or import cache without an explicit policy decision; emit clear missing-asset diagnostics.

Validation: fresh worktree bootstrap, Godot import, main_3d baseline health, parser/resource/import error counts.

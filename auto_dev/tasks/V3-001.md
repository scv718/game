# V3-001 Dungeon Completion & Return Loop

Purpose: complete the existing preparation → arena → MercenaryActor3D combat path through victory/defeat, one-time outcome, reward, return, cleanup, and Threat/Wave reporting.

Reuse: DungeonManager, DungeonPreparationManager, DungeonRuntime, MercenaryRoster, VillageResources, DeathLedger, WaveManager.

Constraints: no second combat system, ledger, mercenary model, or invented dungeon effect.

Reference note: DungeonRuntime is NOT an autoload — it is a child node of main_3d.tscn (or dungeon arena tree). Do not probe it via root.get_node_or_null("/root/DungeonRuntime") in SceneTree headless tests. Existing API to reuse: begin_run / end_run / spawn_party / spawn_enemies_for_dungeon / is_run_active / get_returned_alive_ids (see scripts/dungeon_runtime.gd).

Validation: victory, defeat, single reward, cleanup, repeated entry, DeathLedger interaction, dungeon regression, baseline 3D health.
